import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const AdminChecklistScreen = () => {
	return (
		<View>
			<Text>This is AdminChecklistScreen</Text>
		</View>
	);
};

const styles = StyleSheet.create({});

export default AdminChecklistScreen;
