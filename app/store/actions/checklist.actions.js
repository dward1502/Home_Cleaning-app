import Checklist from '../../models/checklist'

export const FETCH_ID_CHECKLISTS = 'FETCH_ID_CHECKLISTS';


export const fetchChecklists = (id) => {
  return async (dispatch,getState) => {
    try{

    }catch(error) {
      throw error
    }
  }
}